#include <iostream>
#include "CommandLine.h"

using namespace std;

int main()
{
    // Creamos un objeto de tipo CommandLine y le decimos que se ejecute
    CommandLine cmd;
    cmd.run();
    return 0;
}
